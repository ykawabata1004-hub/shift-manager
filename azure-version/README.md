# Shift Manager - Firebase版デプロイ手順

このフォルダには、GitHub Pages + Firebase Realtime Databaseで動作するバージョンが含まれています。

## 📋 デプロイ前の準備

### 1. Firebase設定を更新

`js/firebase-config.js` を開き、以下の手順で取得した設定に置き換えてください:

1. [Firebase Console](https://console.firebase.google.com/) にアクセス
2. 新しいプロジェクトを作成
3. Realtime Databaseを有効化（テストモードで開始）
4. プロジェクト設定から Web アプリを追加
5. 表示された設定コードを `firebase-config.js` にコピー

### 2. GitHubにアップロード

1. [GitHub](https://github.com/) で新しいリポジトリを作成（Public）
2. このフォルダ内の全ファイルをアップロード:
   - `index.html`
   - `styles.css`
   - `js/script.js`
   - `js/firebase-config.js`

### 3. GitHub Pagesを有効化

1. リポジトリの Settings → Pages
2. Source: `main` ブランチ、`/ (root)` フォルダ
3. Save をクリック
4. 数分後、公開URLが表示されます

### 4. Microsoft Teamsタブとして追加

1. Teamsチャネルで「+」をクリック
2. 「Website」を選択
3. GitHub PagesのURLを入力
4. タブ名: `Shift Manager`

## 🔒 セキュリティ設定（重要）

Firebase Consoleで Realtime Database のルールを更新:

```json
{
  "rules": {
    ".read": true,
    ".write": true
  }
}
```

## 📝 注意事項

- 元の `shift-manager` フォルダはローカル環境用です
- このフォルダ（`shift-manager-firebase`）はデプロイ用です
- データはFirebaseに保存され、チーム全体で共有されます

## 詳細な手順

詳しい手順は `deployment_guide.md` を参照してください。
